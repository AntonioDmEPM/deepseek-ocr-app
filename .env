# Device Configuration
# Options: 'cpu', 'gpu', 'auto'
# - 'cpu': Force CPU usage (compatible with all systems)
# - 'gpu': Use GPU if available, fallback to CPU
# - 'auto': Automatically detect and use best available device
DEVICE_MODE=cpu

# Flash Attention (GPU only, requires flash-attn package)
# Set to 'true' only if you have:
# - NVIDIA GPU with CUDA support
# - flash-attn==2.7.3 installed
# - CUDA 11.8 or compatible version
USE_FLASH_ATTENTION=false

# Server Configuration
HOST=0.0.0.0
PORT=5001
DEBUG=true
